[Unit]
Description=Fuel Patrol Client
After=network.target

[Service]
User=root
Group=www-data
WorkingDirectory=/root/dev/fuel_patrol_client
Environment="PATH=/root/.local/bin:/usr/local/bin:/usr/bin:/bin"
ExecStart=pnpm run dev
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
